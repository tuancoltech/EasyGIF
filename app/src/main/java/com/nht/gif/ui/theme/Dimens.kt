package com.nht.gif.ui.theme

import androidx.compose.ui.unit.dp

/** Project-wide spacing tokens. Mirror @dimen/ resources for Compose screens. */
object Dimens {
    val contentPadding = 16.dp
    /** Matches @dimen/divider_thickness (1px ≈ 1dp hairline). */
    val dividerThickness = 1.dp
}
